import React from "react";
import { Text, View } from "react-native";

const Settings = () => {
  return (
    <View>
      <Text className="text-foreground">
        You're currently on the settings page!
      </Text>
    </View>
  );
};

export default Settings;
